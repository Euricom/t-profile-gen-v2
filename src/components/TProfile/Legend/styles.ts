import styled from 'styled-components';

export const Legend = styled.ul`
  bottom: 0;
  position: absolute;
  right: 0;
`;

export const LegendItem = styled.li`
  font-size: 0.85rem;
  margin: 0.1rem 0;
  span {
    font-weight: 700;
  }
`;
